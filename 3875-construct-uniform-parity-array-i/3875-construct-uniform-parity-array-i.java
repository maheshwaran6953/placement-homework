class Solution {
    public boolean uniformArray(int[] nums1) {
        // int n = nums1.length, nums2[] = new int[n];
        // if(n == 0) return false;
        // boolean found = false;
        // for(int i = 1; i < n; i++){
        //     if(i == n && nums1[i] % 2 != 0 && !found) return false;
        //     if((nums1[i - 1] - nums1[i]) % 2 != 0){
        //         return false;
        //     } else {
        //         found = true;
        //     }
        // }
        return true;
    }
}